# Copy to .env and fill in. Never commit .env.
# This file lists variable NAMES only — no real values, no real keys.

# LLM provider (abstracted — no provider chosen yet, see docs/architecture/ARCHITECTURE.md)
LLM_API_KEY=

# CRM integrations (Kommo / Pipedrive) — placeholders, not wired up yet
CRM_API_KEY=
CRM_API_BASE_URL=

# App
ENV=development
LOG_LEVEL=info
